# Query System Architecture

## System Flow Diagram

```
┌─────────────────────────────────────────────────────────────────────┐
│                          MOTHER INTERFACE                           │
├─────────────────────────────────────────────────────────────────────┤
│                                                                     │
│  ┌──────────────────┐      ┌──────────────────┐                   │
│  │  Create Query    │      │  View My Queries │                   │
│  │  Form            │      │  List            │                   │
│  └────────┬─────────┘      └────────┬─────────┘                   │
│           │                         │                              │
│           │ POST                    │ GET                          │
│           │ /api/queries/create     │ /api/queries/my-queries      │
│           │                         │                              │
└───────────┼─────────────────────────┼──────────────────────────────┘
            │                         │
            ▼                         ▼
┌─────────────────────────────────────────────────────────────────────┐
│                        FLASK BACKEND (app.py)                       │
├─────────────────────────────────────────────────────────────────────┤
│                                                                     │
│  ┌────────────────────────────────────────────────────────────┐   │
│  │              routes/queries.py (Blueprint)                  │   │
│  │                                                             │   │
│  │  ├─ POST   /api/queries/create                            │   │
│  │  ├─ GET    /api/queries/my-queries                        │   │
│  │  ├─ GET    /api/queries/all                               │   │
│  │  ├─ POST   /api/queries/{id}/reply                        │   │
│  │  ├─ PUT    /api/queries/{id}/update-status               │   │
│  │  ├─ GET    /api/queries/{id}                             │   │
│  │  └─ GET    /api/queries/statistics                       │   │
│  └────────────────────┬───────────────────────────────────────┘   │
│                       │                                            │
│                       │ Uses functions from                        │
│                       ▼                                            │
│  ┌────────────────────────────────────────────────────────────┐   │
│  │                    models.py                                │   │
│  │                                                             │   │
│  │  • create_query()                                          │   │
│  │  • get_queries_by_mother()                                 │   │
│  │  • get_all_queries()                                       │   │
│  │  • add_reply_to_query()                                    │   │
│  │  • update_query_status()                                   │   │
│  │  • get_query_by_id()                                       │   │
│  └────────────────────┬───────────────────────────────────────┘   │
│                       │                                            │
└───────────────────────┼────────────────────────────────────────────┘
                        │
                        │ MongoDB Operations
                        ▼
┌─────────────────────────────────────────────────────────────────────┐
│                         MONGODB DATABASE                            │
├─────────────────────────────────────────────────────────────────────┤
│                                                                     │
│  ┌──────────────────────────────────────────────────────────┐     │
│  │  queries Collection                                       │     │
│  │                                                           │     │
│  │  {                                                        │     │
│  │    _id: ObjectId,                                        │     │
│  │    motherId: ObjectId,                                   │     │
│  │    subject: "Question about iron",                       │     │
│  │    message: "What are good sources...",                  │     │
│  │    category: "nutrition",                                │     │
│  │    status: "pending",                                    │     │
│  │    replies: [                                            │     │
│  │      {                                                   │     │
│  │        doctorId: "...",                                  │     │
│  │        message: "Include spinach...",                    │     │
│  │        repliedAt: DateTime                               │     │
│  │      }                                                   │     │
│  │    ],                                                    │     │
│  │    createdAt: DateTime                                   │     │
│  │  }                                                       │     │
│  └──────────────────────────────────────────────────────────┘     │
│                                                                     │
│  Indexes:                                                          │
│  • motherId + createdAt                                           │
│  • status + createdAt                                             │
│  • category + status                                              │
│  • doctorId + createdAt                                           │
│                                                                     │
└─────────────────────────────────────────────────────────────────────┘
                        ▲
                        │ MongoDB Operations
                        │
┌───────────────────────┼────────────────────────────────────────────┐
│                       │         FLASK BACKEND                      │
│  ┌────────────────────┴───────────────────────────────────────┐   │
│  │                    models.py                                │   │
│  └────────────────────┬───────────────────────────────────────┘   │
│                       │                                            │
│                       │ Provides data to                           │
│                       ▼                                            │
│  ┌────────────────────────────────────────────────────────────┐   │
│  │              routes/queries.py (Blueprint)                  │   │
│  └────────────────────┬───────────────────────────────────────┘   │
│                       │                                            │
└───────────────────────┼────────────────────────────────────────────┘
                        │
            ┌───────────┴────────────┐
            │ JSON Response          │
            ▼                        ▼
┌─────────────────────┐    ┌─────────────────────┐
│  MOTHER INTERFACE   │    │  DOCTOR INTERFACE   │
├─────────────────────┤    ├─────────────────────┤
│                     │    │                     │
│ • View queries      │    │ • View all queries  │
│ • See replies       │    │ • Reply to queries  │
│ • Create new query  │    │ • Update status     │
│ • Check statistics  │    │ • Assign queries    │
│                     │    │ • View statistics   │
└─────────────────────┘    └─────────────────────┘
```

## Data Flow - Creating a Query

```
1. Mother fills form on mother.html
        │
        ▼
2. JavaScript sends POST /api/queries/create
        │
        ▼
3. queries.py validates request
        │
        ▼
4. models.create_query() called
        │
        ▼
5. Insert into MongoDB queries collection
        │
        ▼
6. Return created query as JSON
        │
        ▼
7. Frontend displays success message
```

## Data Flow - Doctor Replying

```
1. Doctor views query list on doctor.html
        │
        ▼
2. JavaScript fetches GET /api/queries/all
        │
        ▼
3. Queries displayed with reply button
        │
        ▼
4. Doctor writes reply, clicks send
        │
        ▼
5. POST /api/queries/{id}/reply
        │
        ▼
6. models.add_reply_to_query() called
        │
        ▼
7. MongoDB updates query document
   - Adds reply to replies array
   - Updates status
   - Sets updatedAt
        │
        ▼
8. Return updated query as JSON
        │
        ▼
9. Frontend refreshes query list
```

## Component Interaction

```
┌──────────────┐
│   Frontend   │ (Your teammate's responsibility)
│  (HTML/JS)   │
└──────┬───────┘
       │ HTTP Requests (JSON)
       │
       ▼
┌──────────────────────────────────────┐
│           Backend API                │ (Your implementation)
│  ┌────────────────────────────────┐  │
│  │  routes/queries.py             │  │
│  │  - Handle requests             │  │
│  │  - Validate data               │  │
│  │  - Call model functions        │  │
│  │  - Return JSON responses       │  │
│  └────────────┬───────────────────┘  │
│               │                      │
│  ┌────────────▼───────────────────┐  │
│  │  models.py                     │  │
│  │  - Database operations         │  │
│  │  - Business logic              │  │
│  │  - Data transformations        │  │
│  └────────────┬───────────────────┘  │
└───────────────┼──────────────────────┘
                │
                ▼
       ┌────────────────┐
       │    MongoDB     │
       │   Database     │
       └────────────────┘
```

## Authentication Flow

```
User logs in
    ↓
Session created (Flask session with cookies)
    ↓
Each API request checks:
    - Is user logged in? (session['user_id'])
    - Does user have correct role? (session['role'])
    ↓
If Mother:
    - Can create queries
    - Can view own queries only
    ↓
If Doctor:
    - Can view all queries
    - Can reply to queries
    - Can update query status
```

## Query Lifecycle

```
Created (status: pending)
    ↓
Doctor sees it in pending list
    ↓
Doctor replies (status: in-progress)
    ↓
Doctor provides solution (status: resolved)
    ↓
Query archived (status: closed)
```

## File Organization

```
mothers_nutrition/
└── latest_imp/
    ├── app.py                          # Main Flask app
    │   └── registers queries_bp
    │
    ├── routes/
    │   ├── auth.py                     # Existing auth
    │   └── queries.py                  # NEW - Query endpoints
    │
    ├── models.py                       # Database models
    │   ├── Existing functions
    │   └── NEW query functions:
    │       ├── create_query()
    │       ├── get_queries_by_mother()
    │       ├── get_all_queries()
    │       ├── add_reply_to_query()
    │       └── update_query_status()
    │
    ├── templates/
    │   ├── mother.html                 # Add query UI here
    │   └── doctor.html                 # Add query management here
    │
    ├── test_queries.py                 # NEW - Test script
    ├── setup_query_db.py               # NEW - DB setup
    ├── QUERY_API_DOCUMENTATION.md      # NEW - Full API docs
    ├── IMPLEMENTATION_GUIDE.md         # NEW - How to use
    └── QUICK_REFERENCE.md              # NEW - Quick ref
```

## Security Model

```
┌─────────────────┐
│  HTTP Request   │
└────────┬────────┘
         │
         ▼
┌─────────────────────┐
│ Session Check       │ ─── No user_id? → 401 Unauthorized
│ (Flask @login)      │
└────────┬────────────┘
         │
         ▼
┌─────────────────────┐
│ Role Check          │ ─── Wrong role? → 403 Forbidden
│ (Mother/Doctor)     │
└────────┬────────────┘
         │
         ▼
┌─────────────────────┐
│ Resource Check      │ ─── Mother viewing another's query? → 403
│ (Ownership)         │
└────────┬────────────┘
         │
         ▼
┌─────────────────────┐
│ Process Request     │
└─────────────────────┘
```

This completes the Query System implementation! 🎉
